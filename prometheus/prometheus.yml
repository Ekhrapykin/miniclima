global:
  scrape_interval: 15s
  evaluation_interval: 15s

storage:
  tsdb:
    out_of_order_time_window: 1y

scrape_configs:
  - job_name: 'ebc10-api'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['api:8000']
